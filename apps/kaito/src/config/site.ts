/**
 * 月ノ瀬 直 — サイト設定
 *
 * 未確定の情報はこのファイルに集約しています。
 * 実際の値が決まったら、ここだけ書き換えてください。
 * 未設定（空文字）の項目は、ページ側が自動で別の表示に切り替わります。
 */
export const site = {
  /**
   * お問い合わせ用 Googleフォームの URL。
   * 空文字の間は、ヘッダーの「お問い合わせ」がページ内のお問い合わせ欄へのスクロールになり、
   * お問い合わせ欄のボタンは出さない（リンク切れを作らないため）。
   * URL を入れた瞬間に、ヘッダーとお問い合わせ欄の両方がフォームへのリンクに切り替わる。
   */
  contactFormUrl: '',

  /**
   * 写真。フリー素材（Unsplash・商用利用可）の仮画像。
   * 本番の写真が用意できたら src/public/images/ に置いて、このパスを差し替える。
   * （セラピーのスタイルは写真ではなく KaitoScene.vue の手描きイラストを使っている）
   */
  images: {
    /** ヒーロー背景に帯として敷く写真（沖縄の海） */
    hero: '/images/hero-okinawa.jpg',
  },
} as const
